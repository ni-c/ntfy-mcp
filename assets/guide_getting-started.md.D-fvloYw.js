import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.Cs5aHXsR.js";var i=JSON.parse(`{"title":"Getting started","description":"","frontmatter":{},"headers":[],"relativePath":"guide/getting-started.md","filePath":"guide/getting-started.md","lastUpdated":1788342097000}`),a={name:`guide/getting-started.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="getting-started" tabindex="-1">Getting started <a class="header-anchor" href="#getting-started" aria-label="Permalink to “Getting started”">​</a></h1><h2 id="requirements" tabindex="-1">Requirements <a class="header-anchor" href="#requirements" aria-label="Permalink to “Requirements”">​</a></h2><ul><li>Node.js ≥ 22</li><li>A reachable <strong>ntfy</strong> server. Credentials are optional: an instance that allows anonymous access needs none.</li></ul><h2 id="run-it" tabindex="-1">Run it <a class="header-anchor" href="#run-it" aria-label="Permalink to “Run it”">​</a></h2><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">NTFY_URL</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">https://ntfy.example.net</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;"> NTFY_TOPICS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">alerts</span><span style="--shiki-light:#953800;--shiki-dark:#FFA657;"> npx</span><span style="--shiki-light:#0550AE;--shiki-dark:#79C0FF;"> -y</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> @ni-c/ntfy-mcp</span></span></code></pre></div><p><code>NTFY_URL</code> is the only required variable, and it has no default. <code>https://ntfy.sh</code> would be the obvious one and the worst possible one — a misconfigured server would publish to the public internet rather than failing.</p><p>With credentials:</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">NTFY_URL</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">https://ntfy.example.net</span><span style="--shiki-light:#953800;--shiki-dark:#FFA657;"> \\</span></span>
<span class="line"><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">  NTFY_TOKEN=tk_…</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;"> \\</span></span>
<span class="line"><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">  NTFY_TOPICS=alerts,deploys</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;"> \\</span></span>
<span class="line"><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">  npx</span><span style="--shiki-light:#0550AE;--shiki-dark:#79C0FF;"> -y</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> @ni-c/ntfy-mcp</span></span></code></pre></div><p>Use <code>NTFY_USERNAME</code> and <code>NTFY_PASSWORD</code> instead where the account has no token — <code>ntfy user add</code> produces a username and a password, not a token. Setting both forms at once stops the server at startup; see <a href="/guide/configuration#credentials">Configuration</a>.</p><p>Without configuration the server still starts and lists its tools, so registries and inspectors can introspect it; every call then fails with setup instructions instead of reaching the API.</p><h2 id="writes-are-on" tabindex="-1">Writes are on <a class="header-anchor" href="#writes-are-on" aria-label="Permalink to “Writes are on”">​</a></h2><p><code>NTFY_READ_ONLY</code> defaults to <code>false</code>, so the seven write tools are registered. ntfy exists to publish, and a read-only default would ship a notification server that cannot notify. Two things follow from that direction, and both are covered under <a href="/guide/configuration#read-only-and-why-it-defaults-to-false">Configuration</a>.</p><h2 id="the-first-call-worth-making" tabindex="-1">The first call worth making <a class="header-anchor" href="#the-first-call-worth-making" aria-label="Permalink to “The first call worth making”">​</a></h2><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>Is the ntfy server reachable, and can you post to the alerts topic?</span></span></code></pre></div><p><code>get_server_info</code> reads <code>/v1/health</code>, <code>/v1/config</code> and <code>/v1/stats</code>, which are public on a default instance — so it is the one tool that answers before the credentials are right. It also reports whether the account is an admin, which decides whether the user and access tools are worth trying at all.</p><p><code>check_topic_access</code> then tests the topic. Read its answer carefully: it tests the <strong>read</strong> side, and a write-only publishing token is denied there while publishing works perfectly well. That combination is the single most common source of confusion with ntfy, and the tool says so in its own result.</p><h2 id="then-publish-something" tabindex="-1">Then publish something <a class="header-anchor" href="#then-publish-something" aria-label="Permalink to “Then publish something”">​</a></h2><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>Send me a notification on the alerts topic when the test suite finishes.</span></span></code></pre></div><p>Keep the id that comes back. Passing it to <code>update_message</code> as <code>sequence_id</code> revises that notification in place instead of sending a second one — and asks you first, because the revision replaces the message on the devices that already have it.</p>`,19)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};