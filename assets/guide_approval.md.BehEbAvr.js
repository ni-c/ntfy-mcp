import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.Cs5aHXsR.js";var i=JSON.parse(`{"title":"Asking a person","description":"","frontmatter":{},"headers":[],"relativePath":"guide/approval.md","filePath":"guide/approval.md","lastUpdated":1788342097000}`),a={name:`guide/approval.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="asking-a-person" tabindex="-1">Asking a person <a class="header-anchor" href="#asking-a-person" aria-label="Permalink to “Asking a person”">​</a></h1><p>Five of the thirteen tools change who can reach this instance, or take notifications away from people who may not have read them yet. All five <strong>ask a person first</strong>.</p><p>Not a <code>confirm: true</code> argument the model can set. Not a token the model reads out of its own previous result. A dialog, raised through <a href="https://modelcontextprotocol.io/specification/2025-11-25/client/elicitation" target="_blank" rel="noreferrer">MCP elicitation</a>, that goes to the client and is shown to whoever is sitting there.</p><p>The specification says a client <em>should</em> keep a human in the loop:</p><blockquote><p>there <strong>SHOULD</strong> always be a human in the loop with the ability to deny tool invocations</p></blockquote><p>This server does not rely on that. It raises the question itself, and until an answer comes back, nothing happens.</p><h2 id="what-asks-and-when" tabindex="-1">What asks, and when <a class="header-anchor" href="#what-asks-and-when" aria-label="Permalink to “What asks, and when”">​</a></h2><table tabindex="0"><thead><tr><th>Tool</th><th>When it asks</th></tr></thead><tbody><tr><td><code>delete_messages</code></td><td>always</td></tr><tr><td><code>delete_user</code></td><td>always</td></tr><tr><td><code>manage_user_access</code></td><td>always</td></tr><tr><td><code>create_user</code></td><td>always</td></tr><tr><td><code>update_message</code></td><td>always</td></tr><tr><td>everything else</td><td>never</td></tr></tbody></table><p><code>create_user</code> is the odd one on that list, because it destroys nothing. It is the mirror image of <code>delete_user</code>: bringing an account into existence is a change to who may reach this instance, and no annotation carries that — <code>destructiveHint</code> is about what a call takes away. Its own annotation has said “which is why it is guarded” since the tool was written, and it was not.</p><p>Publishing is deliberately <strong>not</strong> on the list. Sending a notification destroys nothing, and it reaches people who cannot un-receive it — an outbound effect, not a destructive one. The control for that is <code>NTFY_TOPICS</code>, which bounds where this server may publish at all, and an ntfy account scoped to those topics behind it. A dialog before every notification would be how people learn to tick without reading.</p><p><code>update_message</code> looks like publishing&#39;s sibling and is not, which is why that argument stops before it. From ntfy 2.16 a revision replaces the notification <strong>on the subscribers&#39; devices</strong>: the text they were shown is gone everywhere but this server&#39;s cache, so it takes something away rather than only adding one. It also carries the same content schema as <code>publish_message</code>, <code>actions</code> included, and an <code>http</code> button fires from the recipient&#39;s phone with a method, headers and body the caller chose. What is confirmed is the notification, not the new text — binding the content would ask again for every corrected typo while proving nothing.</p><h2 id="what-the-dialog-contains" tabindex="-1">What the dialog contains <a class="header-anchor" href="#what-the-dialog-contains" aria-label="Permalink to “What the dialog contains”">​</a></h2><p>Topics, counts and usernames. Never the content of a notification, which was written by whoever could publish to the topic — on an open instance, anyone who knows its name.</p><p>And never the <strong>password</strong> <code>create_user</code> was given. It is a live credential, and the prompt is read back by a person and by a model alike, so it is in neither the text nor the token&#39;s binding. A consequence worth knowing: on the fallback path a token issued for one password works with another, because what the approval is about is the account name.</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>This will create the account &quot;publisher&quot;.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>It becomes an account on this instance. Nothing is reachable through it until</span></span>
<span class="line"><span>manage_user_access grants a topic — but whoever has the password can then</span></span>
<span class="line"><span>authenticate as it.</span></span></code></pre></div><p>The approval is bound to its target, so one obtained for a call cannot be replayed against another. For a <em>set</em> of targets the binding is a fingerprint of the exact list: an approval for <code>[&quot;a&quot;]</code> does not execute <code>[&quot;a&quot;, &quot;b&quot;]</code>.</p><h2 id="clients-that-cannot-show-a-dialog" tabindex="-1">Clients that cannot show a dialog <a class="header-anchor" href="#clients-that-cannot-show-a-dialog" aria-label="Permalink to “Clients that cannot show a dialog”">​</a></h2><p>Not every MCP client implements elicitation, and a stateless gateway may not be able to speak for the one it is currently serving. Rather than refuse to work — which pushes people towards switching the guard off entirely — the tool falls back to a <strong>two-call token</strong>: the first call returns a random string, the second has to quote it back.</p><p>Be clear about what that proves, because this server is:</p><blockquote><p>the token proves the call was made twice with the same arguments, and nothing more.</p></blockquote><p>A model can read the token out of the first result and call again in the same turn without anybody seeing it. It catches a widened target set; it does not catch a model that was talked into the whole thing. The fallback text says so rather than implying somebody approved.</p><h2 id="switching-the-dialog-off" tabindex="-1">Switching the dialog off <a class="header-anchor" href="#switching-the-dialog-off" aria-label="Permalink to “Switching the dialog off”">​</a></h2><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">ELICITATION</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">false</span></span></code></pre></div><p>Default is <code>true</code>. <code>false</code> does <strong>not</strong> remove the guard — it takes the fallback path above, which means the token. There is no setting in which a guarded call goes unannounced.</p><p>Use it where a dialog is the wrong shape rather than an unwanted one: a scheduled job, a test harness, a client whose dialog interrupts something else.</p><div class="warning custom-block"><p class="custom-block-title">It is deliberately not prefixed</p><p><code>ELICITATION</code> has no <code>NTFY_</code> in front of it, so one <code>export ELICITATION=false</code> — or one <code>-e ELICITATION=false</code> in a compose file — reaches <strong>every</strong> MCP server in that environment, not just this one. That is the point of it and also its risk.</p><p>Two things make it visible rather than silent:</p><ul><li><p>a server started with it off prints one line at startup, in the log of every server it actually reached:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>ntfy-mcp: ELICITATION=false — guarded tools fall back to the two-call token</span></span></code></pre></div></li><li><p>the fallback text names the server that did not ask, instead of blaming a client that was working fine. :::</p></li></ul><p>Anything other than <code>true</code> or <code>false</code> — <code>1</code>, <code>off</code>, <code>yes</code> — <strong>stops the server</strong> with exit code 1 and a message naming both valid values. This is the only variable in this family that defaults to <em>on</em>: a typo that fell back to the default would leave the dialog running while the operator believed it was off, and there would be nothing to tell them.</p><h2 id="annotations-are-the-other-half-and-they-are-only-a-hint" tabindex="-1">Annotations are the other half, and they are only a hint <a class="header-anchor" href="#annotations-are-the-other-half-and-they-are-only-a-hint" aria-label="Permalink to “Annotations are the other half, and they are only a hint”">​</a></h2><p>Every tool of this server declares all four MCP tool annotations — <code>readOnlyHint</code>, <code>destructiveHint</code>, <code>idempotentHint</code>, <code>openWorldHint</code> — so a client can tell before it calls what a call would do. See <a href="/reference/tools">Tools</a>.</p><p>They are advice, and the specification says so:</p><blockquote><p>clients <strong>MUST</strong> consider tool annotations to be untrusted unless they come from trusted servers</p></blockquote><p>An annotation is something a client may ignore. The dialog is not: it is enforced here, on the server side, and no answer means no change. The two are different claims — the annotation says what a call <em>does</em>, the dialog decides whether it <em>happens</em> — so the two lists are related but not the same. <code>create_user</code> is the standing example in the other direction: it destroys nothing, and it is guarded.</p><h2 id="an-approval-proves-binding-not-freshness" tabindex="-1">An approval proves binding, not freshness <a class="header-anchor" href="#an-approval-proves-binding-not-freshness" aria-label="Permalink to “An approval proves binding, not freshness”">​</a></h2><p>The state that carries a dialog&#39;s answer back is sealed, and the seal proves the question was this server&#39;s and named this target. It does not prove the answer is being used for the first time: it carries no nonce, and verifying it spends nothing, so the same answer can be submitted again until it expires. Whoever can do that received the question in the first place and is the client — this is not a route around the person — but it does mean <strong>at-most-once is not guaranteed on the dialog path</strong>. The two-call token is the opposite: it is a secret the server keeps and deletes on use.</p><p>Every guarded tool here is idempotent in effect, so a repeated leg lands on the same instance: <code>create_user</code> and <code>delete_user</code> fail the second time, the access rule is written or removed once, a second delete re-announces a deletion subscribers were already told about, and a replayed <code>update_message</code> re-applies the revision it applied before. <code>publish_message</code> is the one operation that genuinely acts twice, and it is unguarded — so there is no approval state to replay, and ntfy offers no idempotency key that would make it safe to retry blindly. See the <a href="https://github.com/ni-c/ntfy-mcp/blob/main/SECURITY.md" target="_blank" rel="noreferrer">security policy</a>.</p><h2 id="behind-a-gateway" tabindex="-1">Behind a gateway <a class="header-anchor" href="#behind-a-gateway" aria-label="Permalink to “Behind a gateway”">​</a></h2><p>Both protocol revisions are handled from one code path. On <code>2025-11-25</code> the question is pushed to the client; on <code>2026-07-28</code> there is no server→client channel at all, so the call returns <code>input_required</code>, ends, and the client retries carrying the answer.</p><p>That answer arrives as ordinary request content, which the SDK does not validate — so the state that ties an answer to its question is sealed (HMAC). A reply whose seal does not open, or opens onto a different target, counts as <strong>no answer</strong> and produces a fresh question rather than an error. The likeliest cause is not an attack: it is a gateway that put the server to sleep while the person was reading.</p><p>If you run this behind <a href="https://github.com/ni-c/mcp-hub" target="_blank" rel="noreferrer">mcp-hub</a>, the hub passes elicitation through in both directions; see its <a href="https://ni-c.github.io/mcp-hub/guide/elicitation" target="_blank" rel="noreferrer">elicitation guide</a>.</p></div>`,26)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};