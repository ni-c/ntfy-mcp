import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.Cs5aHXsR.js";var i=JSON.parse(`{"title":"Changelog","description":"","frontmatter":{},"headers":[],"relativePath":"reference/changelog.md","filePath":"reference/changelog.md","lastUpdated":1787823347000}`),a={name:`reference/changelog.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to “Changelog”">​</a></h1><h2 id="_0-2-0-2026-09-03" tabindex="-1">[0.2.0] - 2026-09-03 <a class="header-anchor" href="#_0-2-0-2026-09-03" aria-label="Permalink to “[0.2.0] - 2026-09-03”">​</a></h2><h3 id="added" tabindex="-1">Added <a class="header-anchor" href="#added" aria-label="Permalink to “Added”">​</a></h3><ul><li><p>Every tool declares an <code>outputSchema</code> and answers with <code>structuredContent</code> beside the text block. A client no longer has to parse prose to use a result.</p><p>The four tools that report what someone else wrote — <code>list_messages</code>, <code>get_message</code>, <code>get_account</code>, <code>list_users</code> — carry <code>untrusted: true</code> and <code>source: &quot;ntfy&quot;</code> as fields of the object as well as in the text. A client that reads the structured half and ignores the text would otherwise receive a publisher&#39;s title and body with no framing at all, and the framing is the guard. <code>get_server_info</code> does not carry the marker: its sections are the instance&#39;s own configuration and counters.</p><p>What this server builds is described exactly; what it passes on from ntfy is declared as an object with no fixed shape. The SDK validates every result against the schema before it goes out, so a schema stricter than the data would turn an upstream release that adds a field into a tool that fails outright rather than one that reports a field nobody expected.</p></li><li><p>Tools that need a confirmation now <strong>ask the user</strong>, on clients that can show a prompt. The two-call <code>confirm_token</code> remains for clients that cannot, so nothing that works today stops working — but where a person can be asked, one is, instead of a token that only proves the same call was made twice.</p></li><li><p><strong><code>create_user</code> now asks too.</strong> It destroys nothing, which is why it was not guarded and why <code>destructiveHint</code> cannot say what is wrong with it: bringing an account into existence is a change to who may reach this instance. It is the mirror image of <code>delete_user</code>, which was guarded from the start, and its own annotation had claimed &quot;which is why it is guarded&quot; while it was not.</p><p>The <strong>password stays out of it</strong> — out of the prompt, which is read back by a person and by a model, and out of the token&#39;s binding. A consequence worth knowing: on the fallback path a token issued for one password works with another, because what the approval is about is the account name.</p><p>Publishing deliberately stays unguarded. Sending a notification destroys nothing and reaches people who cannot un-receive it — an outbound effect, not a destructive one. <code>NTFY_TOPICS</code> is the control for that.</p></li><li><p><code>ELICITATION</code> switches the dialog off — <code>false</code> sends a client that could have been asked down the two-call-token path instead. For a scheduled job or a test harness, where a dialog is the wrong shape rather than an unwanted one.</p><p>It does <strong>not</strong> remove the guard: there is no setting in which a guarded call goes unannounced. Two deliberate rough edges come with it. The variable is <strong>not prefixed</strong>, so one <code>export ELICITATION=false</code> reaches every MCP server in the environment — which is why a server started with it off prints a line saying so, and why the fallback text names the server instead of blaming a client that was working fine. And a value that is neither <code>true</code> nor <code>false</code><strong>stops the server</strong>: it is the only variable here that defaults to <em>on</em>, so failing open on a typo would leave the dialog running while the operator believed it was off. It is read after <code>NTFY_TOKEN</code> and <code>NTFY_PASSWORD</code> are wiped from the environment, so that exit cannot leave a credential behind.</p></li><li><p>A <code>docs/guide/approval.md</code> page.</p></li></ul><h3 id="changed" tabindex="-1">Changed <a class="header-anchor" href="#changed" aria-label="Permalink to “Changed”">​</a></h3><ul><li><p>The advertised schemas avoid a spelling that is legal JSON Schema and still gets a tool refused, or its constraint silently dropped, by some MCP clients: a value that was left untyped is declared as what it really is. What the tools accept and return is unchanged; only the way the schema says so is.</p></li><li><p><code>manage_user_access</code> reports the <code>action</code> it was asked for on every outcome, not only on <code>revoke</code>. Granting and revoking answered in two different shapes before; the shape is now one.</p></li><li><p><code>get_account</code> against an ntfy that answers with something other than a JSON object returns <code>{}</code> rather than that value. An allowlist keeps nothing it does not understand, and passing the value back forwarded exactly the shape the allowlist exists to filter.</p></li><li><p>The two-call <code>confirm_token</code> prompt is an error result. The operation was asked for and did not happen, and a tool that declares an output schema may not answer without <code>structuredContent</code> unless the result is an error. The text is unchanged and still carries the token.</p></li><li><p>Runs on <strong>MCP SDK 2.0</strong>. Existing clients see the same protocol revision they always did; the change is the package layout behind it, and it is what lets the dialog above work on both protocol eras from one code path — including behind a stateless gateway, where the older mechanism silently fell back to the weaker token for every client.</p></li><li><p>The linter is <strong>oxlint</strong> instead of eslint plus typescript-eslint, which lifts the TypeScript ceiling: typescript-eslint pins <code>typescript</code> below 6.1, so this repository was held on TypeScript 6 by its linter rather than by its code.</p></li><li><p>The tool filter, the confirmation store and the documentation-asset generator now come from <strong><code>mcp-tool-allowlist</code></strong>, <strong><code>mcp-approval</code></strong> and <strong><code>svg-asset-set</code></strong> rather than from copies kept here — 740 fewer lines, and one place to fix each. None of them has a runtime dependency of its own.</p></li><li><p>stdio is served through <code>serveStdio</code>, so the connection&#39;s era is negotiated on the opening exchange rather than assumed. A client that pins the <code>2026-07-28</code> era is served it; until now its <code>server/discover</code> probe was answered with &quot;Method not found&quot; and only <code>2025-11-25</code> was on offer. A client that speaks the older era sees no change — it is still pinned to one instance for the life of the connection, exactly as a hand-wired <code>StdioServerTransport</code> served it.</p></li></ul><h3 id="fixed" tabindex="-1">Fixed <a class="header-anchor" href="#fixed" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><p>Confirmation tokens are compared with a <strong>constant-time</strong> comparison. The copy in this repository used <code>!==</code>, which leaks through timing how much of a guess was right. Reaching a token still requires having received it in a previous tool result, so this closes a margin rather than a hole.</p></li><li><p>A <code>confirm_token</code> that does not match is now refused with the reason — invalid, expired, or issued for different arguments — instead of being answered with a fresh prompt. The second is self-healing when a token merely expired and silent when the token was issued for something else, which is the case the binding exists to catch.</p></li><li><p>An entry in <code>NTFY_ALLOW_TOOLS</code> that is not tool-name-shaped is now <strong>redacted</strong> in the error rather than quoted back. <code>NTFY_TOKEN</code> and <code>NTFY_ALLOW_TOOLS</code> are adjacent lines in every compose file, and a paste into the wrong one used to print the credential into the client&#39;s log.</p></li><li><p>A duplicated comment block in <code>manage_user_access</code> — the paragraph explaining why its resource key is a tuple appeared twice.</p></li></ul><h3 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to “Security”">​</a></h3><ul><li><p><strong><code>NTFY_TOPICS</code> now bounds the access tools, which it had only claimed to.</strong><code>manage_user_access</code> passed its <code>topic</code> argument straight into the request body, so <code>topic: &quot;*&quot;</code> handed an account permanent read-write access to every topic on the instance — from a server the operator had restricted to one. A grant pattern is now resolved against the list, and where the list is set a wildcard is refused outright: a pattern also covers topics that do not exist yet, and no finite allowlist covers that. The refusal happens before the approval dialog, so a pattern that will not be accepted never becomes a question somebody might tick.</p><p><code>list_users</code> had the same hole from the other side. <code>GET /v1/users</code> is the one endpoint on ntfy that answers &quot;which topics exist here&quot;, for every account at once, and its grant patterns were forwarded verbatim — so a server restricted to <code>alerts</code> was handing back the names of every other topic on the box, and a topic name on ntfy is a bearer credential. Grants are now restated against the allowed topics: a wildcard grant appears once per allowed topic it covers, one that covers none of them is dropped, and the account itself is still listed. The tool&#39;s own <code>topic</code> filter is resolved against the list too.</p></li><li><p><strong><code>update_message</code> now asks a person.</strong> From ntfy 2.16 an update replaces the notification <strong>on the subscribers&#39; devices</strong>, so the text they were shown survives nowhere but this server&#39;s cache — and the tool carries the whole content schema, <code>actions</code> included, where an <code>http</code> button fires from the recipient&#39;s phone with a method, headers and body the caller chose. One unguarded call could turn a delivered alert into a button that calls something. It is gated exactly like <code>delete_messages</code>, on a fingerprint of <code>(topic, sequence_id)</code>.</p><p>The new content is deliberately <strong>not</strong> part of that fingerprint. What is confirmed is the notification; binding the text would ask again for every corrected typo while proving nothing, since the replacement is only reachable through the same tool call.</p><p>Publishing stays unguarded, and the argument for that now stops where it should. Sending a notification destroys nothing; replacing one that people already have does.</p></li><li><p><strong><code>get_account</code> answers from an allowlist instead of a denylist.</strong> It removed the access token values and <code>sync_topic</code> and spread the rest, which meant <code>phone_numbers</code>, the <code>billing</code> block with its Stripe identifiers, and the <code>reservations</code> and <code>subscriptions</code> arrays — each a list of topic names — all reached the transcript, while the README listed those very features under &quot;not implemented, on purpose&quot;. The tool now projects <code>username</code>, <code>role</code>, <code>tier</code>, <code>limits</code>, <code>stats</code>, <code>language</code> and each token&#39;s <code>label</code>, <code>last_access</code> and <code>expires</code>, and drops everything else. A field a newer or forked ntfy adds is dropped without an edit here, which is the property a denylist cannot have.</p></li><li><p><strong><code>NTFY_READ_ONLY</code> accepts <code>1</code> and <code>yes</code>, in any case.</strong> It compared against the literal string <code>true</code>, so <code>NTFY_READ_ONLY=1</code> registered every write tool while the operator believed the server was read-only — and said nothing, because nothing is printed for a variable that parsed to false. A switch that <em>adds</em> a protection is now parsed generously; <code>NTFY_INSECURE_TLS</code>, which <em>removes</em> one, still takes nothing but the exact word. A genuine typo such as <code>=ture</code> still fails open, because the default is <code>false</code>.</p></li><li><p><strong>The residual risk that an approval proves binding and not freshness</strong> is now written down rather than implied. The sealed state carries no nonce, so a retried leg can run an approved operation twice; every guarded tool here is idempotent in effect, and <code>publish_message</code> — the one operation that genuinely acts twice — is unguarded and has no idempotency key on ntfy&#39;s side. See <code>SECURITY.md</code>.</p></li></ul><h2 id="_0-1-0-2026-08-29" tabindex="-1">[0.1.0] - 2026-08-29 <a class="header-anchor" href="#_0-1-0-2026-08-29" aria-label="Permalink to “[0.1.0] - 2026-08-29”">​</a></h2><h3 id="added-1" tabindex="-1">Added <a class="header-anchor" href="#added-1" aria-label="Permalink to “Added”">​</a></h3><ul><li>Initial release: an MCP server for <a href="https://ntfy.sh" target="_blank" rel="noreferrer">ntfy</a>, covering publishing, reading the message cache, and user and topic-access administration.</li><li>A multi-architecture container image at <code>ghcr.io/ni-c/ntfy-mcp</code> (amd64 and arm64), published with an SBOM and build provenance. It runs as an unprivileged user with no npm in the runtime layer and speaks stdio only, so it needs <code>-i</code> and exposes no port.</li><li>Thirteen tools. Six read: <code>list_messages</code>, <code>get_message</code>, <code>check_topic_access</code>, <code>get_server_info</code>, <code>get_account</code>, <code>list_users</code>. Seven write: <code>publish_message</code>, <code>update_message</code>, <code>mark_messages_read</code>, <code>delete_messages</code>, <code>create_user</code>, <code>delete_user</code>, <code>manage_user_access</code>.</li><li><code>NTFY_ALLOW_TOOLS</code> and <code>NTFY_DENY_TOOLS</code> narrow the tool list by name or by a trailing-<code>*</code> prefix, and <code>NTFY_ALLOW_TOOLS=essential</code> selects a curated six that cover publishing and verifying end to end.</li><li><code>NTFY_TOPICS</code> names the topics the server may touch. The first entry is the default when a tool omits one, which keeps a topic name — a bearer secret on a public instance — out of the tool arguments; the list also bounds every read and write tool.</li><li>Basic authentication (<code>NTFY_USERNAME</code> / <code>NTFY_PASSWORD</code>) alongside access tokens (<code>NTFY_TOKEN</code>), because <code>ntfy user add</code> produces a username and password rather than a token. Setting both forms is refused at startup rather than resolved by a precedence rule.</li></ul><h3 id="security-1" tabindex="-1">Security <a class="header-anchor" href="#security-1" aria-label="Permalink to “Security”">​</a></h3><ul><li><code>NTFY_READ_ONLY</code> defaults to <code>false</code>, unlike the same variable in <a href="https://github.com/ni-c/imap-mcp" target="_blank" rel="noreferrer">imap-mcp</a>, where it defaults to <code>true</code>. ntfy exists to publish; a read-only default would ship a notification server that cannot notify. Because of that direction, only the literal string <code>true</code> disables the write tools — a typo leaves them enabled. The destructive tools are gated behind confirmation tokens and ntfy&#39;s own permissions instead, and <code>NTFY_ALLOW_TOOLS=essential</code> or a <code>NTFY_DENY_TOOLS</code> list is the recommended hardening.</li><li><code>delete_messages</code>, <code>delete_user</code> and <code>manage_user_access</code> require a server-generated confirmation token bound to a fingerprint of the exact target, so a confirmation for one target cannot execute another or a longer list.</li><li>Access tokens are removed from <code>get_account</code> output. ntfy returns every token of the account in plaintext there, which would otherwise put a live credential into the conversation transcript. The account&#39;s <code>sync_topic</code> is removed for the same reason — a topic name is a bearer secret.</li><li><code>click</code>, <code>icon</code>, <code>attach</code> and every action button URL are restricted to <code>http:</code> and <code>https:</code>. ntfy stores whatever it is given, and these URLs are opened by the recipient&#39;s device rather than by the server.</li><li>Publishing cannot send email or place a phone call, and no tool creates, reads or exchanges an ntfy access token.</li></ul>`,15)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};