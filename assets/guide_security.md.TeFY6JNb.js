import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.Cs5aHXsR.js";var i=JSON.parse(`{"title":"Security","description":"","frontmatter":{},"headers":[],"relativePath":"guide/security.md","filePath":"guide/security.md","lastUpdated":1788342097000}`),a={name:`guide/security.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to “Security”">​</a></h1><p>This page is the prose version of <a href="https://github.com/ni-c/ntfy-mcp/blob/main/SECURITY.md" target="_blank" rel="noreferrer">SECURITY.md</a>.</p><h2 id="trust-model" tabindex="-1">Trust model <a class="header-anchor" href="#trust-model" aria-label="Permalink to “Trust model”">​</a></h2><p>The credentials this server holds are ntfy credentials, and what they grant depends entirely on what the ntfy account is allowed to do — so the answer is &quot;as much as you gave it&quot;, and the useful advice is to give it little.</p><ul><li><strong>A publishing token</strong> can send notifications to the topics it is granted. Those notifications reach real devices, so the practical damage is noise, plausible phishing in your own alerting channel, and — if the account also has read access — a way to move data off the instance by publishing it to a topic somebody else is subscribed to.</li><li><strong>An admin account</strong> can enumerate every account on the instance, create accounts, and grant or revoke access to any topic. That is the whole authorization model of the ntfy server. Do not give this server an admin account unless you are actually using the user and access tools, and prefer a dedicated non-admin account with <code>write-only</code> access to the topics it needs.</li></ul><p>Treat every environment variable this server reads as a secret. The MCP client process, and therefore the model driving it, sees every tool result.</p><h2 id="a-topic-name-is-a-bearer-credential" tabindex="-1">A topic name is a bearer credential <a class="header-anchor" href="#a-topic-name-is-a-bearer-credential" aria-label="Permalink to “A topic name is a bearer credential”">​</a></h2><p>On an instance with the default <code>auth-default-access</code>, anyone who knows a topic name can subscribe to it and publish to it. There is no membership to check and nothing to revoke — the name is the credential.</p><p>Two things follow. Set <code>NTFY_TOPICS</code> so this server can only touch the topics you meant, and treat the names the way you would treat a password. The server does its part: the account&#39;s <code>sync_topic</code> never leaves <code>get_account</code>, and the first <code>NTFY_TOPICS</code> entry is the default for tools called without a topic, so the name does not have to travel through the tool arguments to be used.</p><p>The list bounds the two account tools as well, which is less obvious than it sounds, because neither of them takes a topic the way a publish does. <code>manage_user_access</code> takes a <em>pattern</em>, and a pattern is a promise about topics that do not exist yet — <code>deploy*</code> covers every future topic starting with those letters and <code>*</code> covers all of them — so where <code>NTFY_TOPICS</code> is set, a wildcard is refused outright and a plain name has to be on the list. <code>list_users</code> takes no topic at all and <em>returns</em> them: an unfiltered <code>/v1/users</code> is the one endpoint on ntfy that answers &quot;which topics exist here&quot;, for every account at once. Its grants are therefore restated against the allowed topics — a wildcard grant appears once per allowed topic it covers, and a grant that covers none of them is not shown.</p><h2 id="writes-are-registered-by-default" tabindex="-1">Writes are registered by default <a class="header-anchor" href="#writes-are-registered-by-default" aria-label="Permalink to “Writes are registered by default”">​</a></h2><p><code>NTFY_READ_ONLY</code> defaults to <code>false</code> because ntfy exists to publish. The residual risk that follows is worth stating plainly: a prompt-injected client can publish what it has just read to another topic on the same instance. Confirmation tokens do not help there, because publishing is not a destructive operation. <code>NTFY_TOPICS</code> is the control that does, and an ntfy account scoped to those topics is the control behind it.</p><h2 id="the-confirmation-honestly" tabindex="-1">The confirmation, honestly <a class="header-anchor" href="#the-confirmation-honestly" aria-label="Permalink to “The confirmation, honestly”">​</a></h2><p>Five tools <strong>ask a person</strong> before they act: <code>delete_messages</code>, <code>delete_user</code>, <code>manage_user_access</code>, <code>create_user</code> and <code>update_message</code>.</p><p>Where the MCP client supports elicitation, the question is a <strong>dialog</strong> shown to whoever is sitting there. The model cannot answer it on their behalf, and nothing happens until an answer comes back.</p><p><code>create_user</code> is the odd one on that list, because it destroys nothing. It is the mirror image of <code>delete_user</code>: bringing an account into existence is a change to who may reach this instance, and no annotation carries that — <code>destructiveHint</code> is about what a call takes away.</p><p><code>update_message</code> is on it because of what ntfy ≥ 2.16 does with it. The revision replaces the notification <strong>on the subscribers&#39; devices</strong>, so the text they were shown survives nowhere but this server&#39;s cache, and the tool carries the whole content schema — including <code>actions</code>, where an <code>http</code> button fires from the recipient&#39;s phone with a method, headers and body the caller chose. The argument that leaves publishing unguarded covers publishing and stops there.</p><p>Where the client cannot show a dialog, the tool falls back to a short-lived, single-use token. Be clear about what that proves, because this server is: <strong>the call was made twice with the same arguments, and nothing more.</strong> A model can read the token out of the first result and quote it back in the same turn. The fallback text says so rather than implying somebody approved, and names whether it was the client that could not be asked or the operator who switched the dialog off with <code>ELICITATION=false</code>.</p><p>Either way the approval is bound to a fingerprint of the <strong>exact</strong> target. One issued for one target cannot execute another, cannot execute a longer list of message ids the model chose afterwards, and — for <code>manage_user_access</code>, whose three arguments overlap in vocabulary — cannot execute the same three values in a different order.</p><p>Confirmation prompts never quote content that came from ntfy. They name the topic, the count or the username and nothing else, because that text is read by a model. <code>create_user</code>&#39;s prompt also leaves out the <strong>password</strong>: it is a live credential, and the prompt is read back both by a person and by a model, so it is in neither the text nor the token&#39;s binding.</p><p>One thing an approval does <strong>not</strong> prove is freshness. The sealed state carries no nonce and verifying it spends nothing, so a retried leg or a gateway that re-sends can run an approved operation twice without asking again. Every guarded tool here is idempotent in effect, so that lands on the same instance either way; the one operation that genuinely acts twice is <code>publish_message</code>, which is unguarded, and ntfy has no idempotency key that would change it.</p><p>See <a href="/guide/approval">Asking a person</a> for what the dialog contains, which clients show one, and what <code>ELICITATION=false</code> does and does not change.</p><h2 id="untrusted-content" tabindex="-1">Untrusted content <a class="header-anchor" href="#untrusted-content" aria-label="Permalink to “Untrusted content”">​</a></h2><p>Everything in a notification was written by whoever could publish to the topic, so <code>list_messages</code>, <code>get_message</code>, <code>get_account</code> and <code>list_users</code> frame their results as untrusted <strong>data</strong> rather than as this server speaking. Usernames and grant patterns are included in that: on an instance with signup enabled, anyone on the internet chooses their own username.</p><p><code>get_server_info</code> is the exception, and deliberately so — its four sections are the instance&#39;s own configuration and counters, set by whoever runs the server this client was pointed at, and half of the object is derived locally rather than fetched.</p><h2 id="get-account-and-list-users-answer-from-an-allowlist" tabindex="-1">get_account and list_users answer from an allowlist <a class="header-anchor" href="#get-account-and-list-users-answer-from-an-allowlist" aria-label="Permalink to “get_account and list_users answer from an allowlist”">​</a></h2><p><code>GET /v1/account</code> returns every access token of the account <strong>in plaintext</strong> (verified against ntfy 2.19.2). Each one is overwritten with <code>(redacted)</code> rather than omitted — a caller seeing no <code>token</code> key at all could reasonably read it as &quot;this entry had none&quot;. No tool in this server creates, reads or exchanges a token either, because every such endpoint hands back a live credential that would then live in the conversation transcript.</p><p>The rest of that response is dropped rather than filtered. <code>get_account</code> reports the username, role, tier, limits, usage, language and the metadata of each token, and nothing else — not the phone numbers, not the <code>billing</code> block with its Stripe identifiers, and not the <code>reservations</code> and <code>subscriptions</code> arrays, each of which is a list of topic names. A denylist would have removed only what was known to be sensitive on the day it was written; an allowlist drops the field a newer or forked ntfy adds without an edit here.</p><p><code>list_users</code> projects the four fields it is about for the same reason. This upstream release carries no password hash in the user record, but that is a property of ntfy 2.19.2 rather than of this server.</p><h2 id="urls-must-be-http-or-https" tabindex="-1">URLs must be http or https <a class="header-anchor" href="#urls-must-be-http-or-https" aria-label="Permalink to “URLs must be http or https”">​</a></h2><p><code>click</code>, <code>icon</code>, <code>attach</code> and every action-button URL are restricted to the <code>http:</code> and <code>https:</code> schemes. ntfy stores whatever it is given — 2.19.2 accepted <code>javascript:alert(1)</code> as a <code>click</code> value without comment — and the reason this matters more than in an ordinary fetch guard is that <strong>ntfy does not open these URLs. The recipient&#39;s phone does.</strong> A <code>click</code> or <code>icon</code> is handed to a mobile client and to the web app, and an <code>http</code> action button is an outbound request originating on the recipient&#39;s device with a caller-controlled method, headers and body.</p><p>The check is scheme-only, with no private-address rule, and that is the right shape: a request to a device on the recipient&#39;s own network is the <em>intended</em> use of an <code>http</code> action for home automation, and it never leaves that network.</p><p>The download name of an attachment is checked separately — no path separators, no traversal, no control characters — because it is a filename on somebody else&#39;s device.</p><h2 id="every-publisher-controlled-field-is-bounded" tabindex="-1">Every publisher-controlled field is bounded <a class="header-anchor" href="#every-publisher-controlled-field-is-bounded" aria-label="Permalink to “Every publisher-controlled field is bounded”">​</a></h2><p>ntfy caps the message body at 4096 bytes but not the title or the tag list, so those are capped here. The body cap is enforced client-side too, and not out of politeness: an oversized body is treated by ntfy as an attachment upload, so the error that comes back is <code>40014 attachments not allowed</code>, which says nothing about size and sends whoever reads it looking in the wrong place entirely. The count is in <strong>bytes</strong> — a <code>.max(4096)</code> on characters would pass 4096 emoji, about 16 kB on the wire.</p><p>Topic names are validated before they reach a URL path. ntfy&#39;s failure mode there is unusually bad: a path that does not match its topic pattern falls through to the static file handler, so <code>GET /has.dot/json</code> answers <strong>200 with the web app&#39;s HTML</strong>. Without the check that page is what the model would have to interpret.</p><h2 id="what-is-deliberately-absent" tabindex="-1">What is deliberately absent <a class="header-anchor" href="#what-is-deliberately-absent" aria-label="Permalink to “What is deliberately absent”">​</a></h2><ul><li><strong>Sending email or placing a phone call</strong> from a published message. ntfy supports both; a tool that mails an arbitrary address on model output is a spam relay driven by injectable content, and <code>call</code> places a real, billable call.</li><li><strong>Creating, reading or exchanging an access token.</strong></li><li><strong>Streaming subscriptions</strong> (<code>/sse</code>, <code>/ws</code>, <code>/raw</code>).</li><li><strong>Attachment upload</strong> — by URL only.</li></ul><p>An absent tool cannot be called, which is the same reason <code>NTFY_READ_ONLY=true</code> does not register the write tools rather than refusing them at call time, and why <code>NTFY_DENY_TOOLS</code> cuts along the same line.</p><h2 id="reporting-a-vulnerability" tabindex="-1">Reporting a vulnerability <a class="header-anchor" href="#reporting-a-vulnerability" aria-label="Permalink to “Reporting a vulnerability”">​</a></h2><p>Use <a href="https://github.com/ni-c/ntfy-mcp/security/advisories/new" target="_blank" rel="noreferrer">private vulnerability reporting</a>. Do not open a public issue for an unpatched vulnerability, and do not include real credentials, tokens, hostnames or private configuration in a report.</p>`,41)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};