import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.Cs5aHXsR.js";var i=JSON.parse(`{"title":"Configuration","description":"","frontmatter":{},"headers":[],"relativePath":"guide/configuration.md","filePath":"guide/configuration.md","lastUpdated":1788342097000}`),a={name:`guide/configuration.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to “Configuration”">​</a></h1><p>Everything is an environment variable; there is no config file. See the <a href="/reference/environment">environment variable reference</a> for the full table.</p><h2 id="the-instance-url" tabindex="-1">The instance URL <a class="header-anchor" href="#the-instance-url" aria-label="Permalink to “The instance URL”">​</a></h2><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">NTFY_URL</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">https://ntfy.example.net</span></span></code></pre></div><p>The only required variable, and the only one with <strong>no default</strong>. <code>https://ntfy.sh</code> would be the obvious default and the worst possible one: a server that was misconfigured, or never configured, would publish to the public internet instead of failing.</p><p>A malformed URL, a scheme other than <code>http</code>/<code>https</code>, or credentials embedded in the URL all stop the server at startup. A missing URL does not — the server has to complete the MCP handshake and answer <code>tools/list</code> without it, so registries and sandbox inspectors can introspect it. Every call then returns the setup instructions.</p><p>Use <code>https</code>. Over plain <code>http</code> the credentials cross the network unencrypted — basic auth is base64, not encryption — and the server prints a warning unless the host is loopback, where <code>http://127.0.0.1:8080</code> is a normal local instance and passes without comment.</p><h2 id="credentials" tabindex="-1">Credentials <a class="header-anchor" href="#credentials" aria-label="Permalink to “Credentials”">​</a></h2><p>Two forms, and they are mutually exclusive:</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">NTFY_TOKEN</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">tk_…</span><span style="--shiki-light:#6E7781;--shiki-dark:#8B949E;">                    # an access token</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">NTFY_USERNAME</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">publisher</span><span style="--shiki-light:#6E7781;--shiki-dark:#8B949E;">            # or basic auth, both together</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">NTFY_PASSWORD</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">…</span></span></code></pre></div><p>Basic auth exists here because <code>ntfy user add</code> produces a username and a password rather than a token, so requiring a token would mean a detour through the token API for every self-hosted instance.</p><p>Setting <code>NTFY_TOKEN</code> alongside <code>NTFY_USERNAME</code>/<code>NTFY_PASSWORD</code> is <strong>refused at startup</strong> rather than resolved by a precedence rule: which credential is in force must never be ambiguous. Setting only one of the basic-auth pair is refused for the same reason.</p><p>No credentials at all is a supported state, not an error. An open instance allows publishing and subscribing anonymously, and refusing to work without credentials would be wrong for a large share of deployments. The one shape that earns a warning is anonymous <strong>writes against the public ntfy.sh</strong>, where knowing a topic name is the whole of the access control.</p><p><code>NTFY_TOKEN</code> and <code>NTFY_PASSWORD</code> are deleted from <code>process.env</code> as soon as they have been read, before any other branch runs, so neither is visible to a child process or in <code>/proc/&lt;pid&gt;/environ</code>.</p><h2 id="topics" tabindex="-1">Topics <a class="header-anchor" href="#topics" aria-label="Permalink to “Topics”">​</a></h2><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">NTFY_TOPICS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">alerts,deploys,backups</span></span></code></pre></div><p>One variable does two jobs. The <strong>first entry is the default</strong> for any tool called without a topic, and the <strong>whole list bounds every tool</strong>, read and write alike — a call naming a topic outside it is refused before any request is made.</p><p>The two are deliberately coupled rather than split into a separate default and an allowlist. A topic name is a bearer credential on ntfy, and someone who sets a default without a restriction has bought no safety while believing they have. Coupling them makes both properties arrive together.</p><p>&quot;Every tool&quot; includes the two that administer access. <code>manage_user_access</code> refuses a grant pattern that reaches past the list — <code>*</code> always does, and so does any prefix, because a pattern also covers topics that do not exist yet — and <code>list_users</code> reports each account&#39;s grants against the allowed topics rather than handing back the name of every topic on the instance.</p><p>Leaving it unset means no default and no restriction: every tool then needs an explicit topic, and any topic on the instance is reachable.</p><p>Entries must be valid ntfy topics — 1 to 64 characters of letters, digits, <code>-</code> and <code>_</code>. A bad entry stops the server and is reported <strong>by position, not by value</strong>, because a stray line in a compose file is exactly what lands in this variable and stderr is the host&#39;s log file.</p><h2 id="tls" tabindex="-1">TLS <a class="header-anchor" href="#tls" aria-label="Permalink to “TLS”">​</a></h2><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">NTFY_INSECURE_TLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">true</span></span></code></pre></div><p>Accepts a self-signed certificate. It is scoped to this connection rather than setting <code>NODE_TLS_REJECT_UNAUTHORIZED</code>, so validation stays on for everything else in the process. Prefer a proper internal CA in the trust store.</p><h2 id="read-only-and-why-it-defaults-to-false" tabindex="-1">Read-only, and why it defaults to false <a class="header-anchor" href="#read-only-and-why-it-defaults-to-false" aria-label="Permalink to “Read-only, and why it defaults to false”">​</a></h2><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">NTFY_READ_ONLY</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">true</span><span style="--shiki-light:#6E7781;--shiki-dark:#8B949E;">    # registers the six read tools and nothing else</span></span></code></pre></div><p><strong>It defaults to <code>false</code></strong>, which is the opposite of <a href="https://github.com/ni-c/imap-mcp" target="_blank" rel="noreferrer">imap-mcp</a>, where the same variable defaults to <code>true</code>. ntfy exists to publish; a read-only default would ship a notification server that cannot notify, while a mailbox is an irreplaceable archive. Two consequences follow from that direction:</p><ul><li><strong>A genuine typo still fails open.</strong> <code>true</code>, <code>1</code> and <code>yes</code> are all read as read-only, in any case — a switch that adds a protection is parsed generously on purpose. <code>NTFY_READ_ONLY=ture</code> is none of them, and because the default is permissive it leaves the write tools enabled, where in imap-mcp it fails closed. (<code>NTFY_INSECURE_TLS</code> is strict for the mirror-image reason: it <em>removes</em> a protection, so only the exact word will do.)</li><li><strong>A client that can publish can publish anywhere on the instance</strong> unless you say otherwise. The approval dialog does not help against that — publishing is not a destructive operation, and a dialog before every notification would be how people learn to tick without reading. <code>NTFY_TOPICS</code> is the control that does.</li></ul><h2 id="turning-the-approval-dialog-off" tabindex="-1">Turning the approval dialog off <a class="header-anchor" href="#turning-the-approval-dialog-off" aria-label="Permalink to “Turning the approval dialog off”">​</a></h2><p><code>delete_messages</code>, <code>delete_user</code>, <code>manage_user_access</code>, <code>create_user</code> and <code>update_message</code> ask a person through MCP elicitation before they act. <code>ELICITATION=false</code> takes them to the two-call token instead. It does not remove the guard; there is no setting in which a guarded call goes unannounced.</p><p>The variable deliberately carries no <code>NTFY_</code> prefix, which means it reaches every MCP server in the same environment, and — unlike the booleans above — a value it does not recognise <strong>stops the server</strong> rather than failing open. See <a href="/guide/approval">Asking a person</a>.</p><p>The recommended shape for anything unattended:</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">NTFY_TOPICS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">deploys</span><span style="--shiki-light:#6E7781;--shiki-dark:#8B949E;">                 # the server can only touch this topic</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">NTFY_ALLOW_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">essential</span><span style="--shiki-light:#6E7781;--shiki-dark:#8B949E;">          # or:</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">NTFY_DENY_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">delete_messages,create_user,delete_user,manage_user_access</span></span></code></pre></div><p>On a self-hosted instance, also give the server its own ntfy account with <code>write-only</code> access to exactly the topics it needs. That is the control behind the control: it holds even if the environment is wrong.</p><h2 id="choosing-the-tools-that-load" tabindex="-1">Choosing the tools that load <a class="header-anchor" href="#choosing-the-tools-that-load" aria-label="Permalink to “Choosing the tools that load”">​</a></h2><p>Read-only mode is one cut, along a line this server drew for you. <code>NTFY_ALLOW_TOOLS</code> and <code>NTFY_DENY_TOOLS</code> let you draw your own:</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">NTFY_ALLOW_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">essential</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">NTFY_ALLOW_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">publish_message,list_messages</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">NTFY_DENY_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">delete_*,create_user,manage_user_access</span></span></code></pre></div><p>Why bother, when all of them work: a model chooses the right tool far more reliably from a handful than from a long list, and every tool it can see costs context on every single request. Thirteen tools is the ceiling here, not the floor. If this is the only MCP server in a session, the full set is fine. If it is one of six, it is not.</p><p><strong>The syntax.</strong> Comma-separated entries. An entry is either an exact tool name or a prefix with a trailing <code>*</code> — <code>list_*</code> matches every tool whose name starts with <code>list_</code>. Entries are trimmed and case-insensitive, empty ones are ignored, and an empty value counts as unset. Nothing else is a pattern: <code>*_user</code> and <code>list_*_x</code> are rejected rather than silently matching nothing.</p><p><strong><code>essential</code></strong> is a curated preset: <code>get_server_info</code>, <code>check_topic_access</code>, <code>publish_message</code>, <code>list_messages</code>, <code>get_message</code> and <code>update_message</code>. It is the end-to-end story of pointing a model at ntfy — find out what the instance supports, confirm the topic is usable, send, check that it landed, read it in full, correct it. Four of the six are read tools, so the preset stays a working combination under <code>NTFY_READ_ONLY=true</code> rather than collapsing to something that can only explain why it cannot publish. It is marked per tool in the <a href="/reference/tools">tool reference</a>, from the same constant the filter reads, so the two cannot drift. It composes — naming a tool alongside it puts that one back, and <code>NTFY_DENY_TOOLS</code> takes one away.</p><p><strong>Both together.</strong> <code>NTFY_ALLOW_TOOLS</code> decides what is in; <code>NTFY_DENY_TOOLS</code> is then subtracted from the result. With only a deny list, everything else stays.</p><p><strong>A name that matches nothing stops the server</strong>, with the offending entry and the list of real names. That is deliberate: the alternative is a tool quietly missing from <code>tools/list</code>, and nobody traces an absence back to an environment variable. The same applies to a pattern that matches no tool.</p><p><strong>With read-only mode</strong>, the write tools are not registered at all, so naming one explicitly in <code>NTFY_ALLOW_TOOLS</code> is an error that says so — rather than calling a tool unknown when it plainly exists. A <em>pattern</em> that covers write tools is fine and simply contributes nothing, which is what makes <code>get_*,delete_*</code> a usable template for both kinds of deployment; and <code>NTFY_ALLOW_TOOLS=essential</code> narrows to the read half of the preset.</p><div class="tip custom-block"><p class="custom-block-title">It is the same cut, not a second one</p><p>A filtered tool is never registered, so it is absent from <code>tools/list</code> and unknown to <code>tools/call</code> alike — exactly what <code>NTFY_READ_ONLY</code> does to a write tool. There is no &quot;hidden but callable&quot; state to reason about.</p></div>`,44)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};